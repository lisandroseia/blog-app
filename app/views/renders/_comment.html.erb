<div>
  <p>
    <%= comment.author.name %>:
    <%= comment.text %></p>
          <% if can? :destroy, comment %>
  <%= button_to user_post_comment_path(user_id: comment.post.author_id, post_id: comment.post_id, id: comment.id), class:"delete", method: :delete do %>
    delete
  <% end %>
  <% end %>
</div>